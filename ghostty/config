# Revert: change to "stable" and restart, or re-download from ghostty.org
auto-update-channel = tip

# Disable ligatures (e.g. -> becoming →, != becoming ≠)
font-feature = -liga
keybind = shift+enter=text:\x1b\r
