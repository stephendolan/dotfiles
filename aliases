# vim: set filetype=sh :

# Always use fancy ls
alias ls='ls -al'

# Shorthand calls
alias dc='docker-compose'

# Don't want to accidentally use old `vi` or `vim` commands
alias vi='nvim'
alias vim='nvim'

# Use VSCode Insiders
alias code='cursor'

