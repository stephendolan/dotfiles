interface:
  display_name: "Writing"
  short_description: "Clear, direct prose in Stephen’s voice"
  default_prompt: "Use $writing to draft a concise message in Stephen’s voice."
policy:
  allow_implicit_invocation: true
