#!/usr/bin/env bash
set -euo pipefail

REMOTE_NAME="${1:-}"
[ "${REMOTE_NAME}" = "origin" ] || exit 0

MAIN_SHA="$(awk '$3 == "refs/heads/main" { print $2; exit }')"
[ -n "${MAIN_SHA}" ] || exit 0

"$(git rev-parse --show-toplevel)/ai/scripts/sync-amp-skills.sh" "${MAIN_SHA}"
